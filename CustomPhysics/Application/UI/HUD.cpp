#include "HUD.h"

namespace CustomApplication
{
	bool HUD::Init()
	{
		return true;
	}

	void HUD::Render()
	{
		/*
		if (m_hudShow)
		{
			if (scene->Pause())
			{
				ActiveScreen(PAUSE);
			}
			else
			{
				ActiveScreen(HELP);
			}
		}
		else
		{
			ActiveScreen(EMPTY);
		}
		*/
	}

}


